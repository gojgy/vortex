# Vortex

Vortex - это легковесный и производительный обратный прокси-сервер и балансировщик нагрузки, написанный на Go.

### Ключевые особенности

* **Обратное проксирование:** Маршрутизация запросов к вашим бэкенд-сервисам (апстримам).
* **Балансировка нагрузки:** Распределение трафика между несколькими экземплярами сервиса с помощью алгоритмов `round_robin` и `least_connections`.
* **Кэширование:** Встроенный LRU-кэш для ускорения ответов и снижения нагрузки на бэкенды.
* **Ограничение частоты запросов (Rate Limiting):** Защита сервисов от перегрузок и злоупотреблений.
* **Раздача статических файлов:** Эффективная отдача статики напрямую из файловой системы.
* **Гибкая конфигурация:** Управление поведением через YAML-файл, включая таймауты, перезапись URI и многое другое.
* **Перезагрузка конфигурации "на лету":** Возможность применить новый конфиг без остановки сервера.
* **Мониторинг:** Встроенные метрики в формате Prometheus и готовый дэшборд для визуализации.

## Начало работы

### Требования

* Go (версия 1.21+)
* Make
* Docker и Docker Compose

### Базовый запуск

Для простого локального запуска, который будет отдавать статическую страницу приветствия, достаточно выполнить одну команду в корне проекта:

```sh
make run
```

После этого Vortex будет запущен на `http://localhost:8080`, а его административная панель - на `http://localhost:9090`. Открыв `http://localhost:8080` в браузере, вы увидите страницу "Welcome to Vortex!".

## Демонстрация возможностей

Чтобы попробовать все функции Vortex в действии, подготовлена специальная демонстрационнуая среда. Она поднимает Vortex и несколько бэкенд-сервисов для наглядной иллюстрации.

**1. Запустите демонстрационную среду**

В корне проекта выполните команду:

```sh
docker compose -f ./testing/docker-compose.testing.yaml up --build
```

**2. Откройте терминал или браузер**

Выполняйте следующие `curl`-запросы, чтобы увидеть, как Vortex обрабатывает разные сценарии.

---

### 🔹 Раздача статики

Vortex может отдавать файлы напрямую, не нагружая ваши бэкенды.

```sh
# Запрос к директории вернет ее index.html
curl http://localhost:8080/static/
```

### 🔹 Балансировка нагрузки (Round Robin)

Выполните эту команду несколько раз. Вы увидите, как `instance_id` в ответе будет циклически меняться с `echo-a` на `echo-b`. Это означает, что Vortex поочередно отправляет запросы на разные серверы.

```sh
curl http://localhost:8080/balanced/
```

*Первый ответ:* `{"instance_id":"echo-a", ...}`
*Второй ответ:* `{"instance_id":"echo-b", ...}`
*Третий ответ:* `{"instance_id":"echo-a", ...}`

### 🔹 Кэширование

Первый запрос дойдет до бэкенда, а последующие (в течение 10 секунд) Vortex будет отдавать из своего кэша, что намного быстрее. Обратите внимание на `request_count` в ответе.

```sh
# 1. Первый запрос (Cache MISS). Запомните request_count.
curl http://localhost:8080/cached/

# 2. Второй запрос (Cache HIT). Ответ придет из кэша, request_count не изменится.
curl http://localhost:8080/cached/

# 3. Подождите 11 секунд и сделайте запрос снова.
# Кэш устарел, Vortex снова сходит на бэкенд, и request_count увеличится.
```

### 🔹 Ограничение частоты запросов (Rate Limiter)

Vortex в тестовой конфигурации позволяет сделать только 5 запросов в минуту на эндпоинт `/limited`.

```sh
# Выполните эту команду 6 раз подряд.
# Первые 5 раз вы получите ответ 200 OK.
curl -i http://localhost:8080/limited/

# На 6-й раз Vortex ответит ошибкой 429 Too Many Requests.
```

### 🔹 Таймауты

Бэкенд-сервис настроен так, чтобы отвечать 3 секунды. Но в конфиге Vortex для этого пути установлен таймаут в 2 секунды. Vortex не будет ждать и оборвет соединение.

```sh
# Этот запрос вернет ошибку 502 Bad Gateway примерно через 2 секунды.
curl -i http://localhost:8080/timeout/
```

### 🔹 Перезапись URI

Vortex умеет изменять путь запроса перед отправкой на бэкенд. Сравните поле `request_path` в ответах.

```sh
# Случай 1: `proxy_pass` содержит URI (`http://echo-a/`).
# Префикс локации /strip/ отрезается.
curl http://localhost:8080/strip/some/endpoint

# Случай 2: `proxy_pass` не содержит URI (`http://echo-a`).
# Весь путь запроса добавляется к хосту.
curl http://localhost:8080/no-strip/some/endpoint
```

### 🔹 Дэшборд и метрики

Откройте в браузере **`http://localhost:9090`**. Вы увидите дэшборд с живыми метриками: общее количество запросов, средняя задержка, распределение по кодам ответа и многое другое.
